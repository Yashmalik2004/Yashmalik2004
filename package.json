{
  "name": "github-stats-generator",
  "version": "1.0.0",
  "description": "Static GitHub stats SVG generator — no third-party services",
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generate-stats.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "@types/node": "^22.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
